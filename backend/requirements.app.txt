# Direct runtime dependencies for the backend application.
# This is a direct-dependency manifest — only packages directly imported by
# backend production source or explicitly required to launch the backend are listed.
# Transitive dependencies are resolved by pip at install time.
# A pinned lock file (requirements.lock.txt) will be added in a future package.
#
# Install this file for local development: pip install -r requirements.app.txt
#
# backend/requirements.txt is the Emergent runtime image manifest and may contain
# private packages (litellm, emergentintegrations) or pre-installed system packages
# that are not available on PyPI. Do not use it for normal local development.

# ── ASGI framework ────────────────────────────────────────────────────────────
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.13.4
email-validator==2.3.0

# ── Database ──────────────────────────────────────────────────────────────────
motor==3.3.1
pymongo==4.5.0

# ── Authentication ────────────────────────────────────────────────────────────
PyJWT==2.13.0
bcrypt==4.1.3

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv==1.2.2

# ── File handling ─────────────────────────────────────────────────────────────
openpyxl==3.1.5

# ── Reports ───────────────────────────────────────────────────────────────────
reportlab==4.5.1

# ── Email ─────────────────────────────────────────────────────────────────────
resend==2.30.1
